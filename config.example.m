% config.example.m
% =========================================================
% API Key Configuration Template
% =========================================================
% INSTRUCTIONS:
%   1. Copy this file:  copyfile('config.example.m','config.m')
%   2. Open config.m and paste your actual API key below
%   3. config.m is in .gitignore — it will NEVER be pushed to GitHub
%
% NEVER paste a real key into config.example.m
% NEVER paste a real key directly into any .m file
% =========================================================

function cfg = config()
    % OpenAI / ChatGPT API key
    % Replace the placeholder below with your actual key in config.m
    cfg.openai_api_key = 'YOUR_OPENAI_API_KEY_HERE';

    % Add any other secrets below in the same format
    % cfg.other_key = 'YOUR_OTHER_KEY_HERE';
end
